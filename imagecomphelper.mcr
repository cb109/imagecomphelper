macroscript imagecomphelper
category:"Buelter"
(
    local thisdir = getFileNamePath (getSourceFileName())
    local scriptfile = pathConfig.appendPath thisdir "imagecomphelper.ms"
    fileIn scriptfile
)